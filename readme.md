# Usage:
´´´
./build/bin/
´´´

# Compile:
for normal compilation (requires: git, cmake, make, gcc, clang):
´´´
./compile.sh
´´´

compilation using docker (requires: docker):
´´´
./compile_docker.sh
´´´